# Env files (local only; deploy via CI/GitHub secrets)
.env
.env*.local

# Firebase service account keys (never commit)
*firebase-adminsdk*.json
serviceAccountKey*.json

# Grafana Cloud remote_write token (never commit)
backend/monitoring/prometheus/gc_token

.claude
CLAUDE.md
